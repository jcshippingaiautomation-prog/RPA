# สถานะระบบ — อัปเดต 24 ส.ค. 2569

## ✅ SQL รันแล้วทั้ง 2 ไฟล์ — ตรวจแล้วผ่าน

`declarations.extra_fields` · `declaration_items.extra_fields` · `declaration_templates` (พร้อมคอลัมน์ 3 ระดับ) ครบหมด

## ✅ ทดสอบแล้วผ่าน

| สิ่งที่ทดสอบ | ผล |
|---|---|
| ดึงใบเดิมจาก DCTK → Master | ✅ ค้นด้วย "DKN 22/2026" เจอ · อ่าน 84 ช่องหัวใบ + 1 รายการ (79 ช่อง) · บันทึกลง DB สำเร็จ |
| เลือก Master 3 ระดับ | ✅ DK&N + REFINED BLEACHED → เจอ · สินค้าไม่ตรง → ไม่เดาสุ่ม |
| ช่องใหม่บันทึกลง DB | ✅ `extra_fields` เก็บได้ทั้งระดับใบและระดับรายการ |
| สร้างใบจาก Master | ✅ เติม 41 ช่อง + ก๊อปรายการสินค้า |
| ฟอร์ม 4 หน้า | ✅ 96 + 82 + 113 + 33 = 324 ช่อง · ไม่มี JS error |
| ตัวตรวจกฎ | ✅ จับได้ทั้งช่องขาด · ความยาวเกิน · ยอดกระทบไม่ตรง |
| **RPA รันจริงครบ 3 หน้า** | ✅ ได้ใบร่าง DCTK000035375 · กรอกช่องเสริม 96 ช่อง (13 + 31 + 52) · errors=0 |

## 🐛 บั๊กที่เจอจากการทดสอบจริง (แก้แล้วทั้งหมด)

1. **วันที่จาก DCTK เป็น `05/08/2026` แต่ระบบเก็บ `2026-08-05`** → ตัวเลือกวันที่ได้ `NaN` กรอกไม่ผ่าน (แก้: แปลงรูปแบบตอนดึง Master + รองรับ พ.ศ.)
2. **ชื่อลูกค้าใช้ชื่อบริษัทเต็มจาก DCTK** (48 ตัว) → เกิน 35 ที่กรมฯ รับ และ RPA ค้นบริษัทไม่เจอ (แก้: ใช้คำค้นจาก `PULL_CUSTOMER` แทน)
3. **นับรายการสินค้าซ้ำ** — Kendo grid ที่มีคอลัมน์ตรึง render 2 ตาราง (แก้: ตัดรายการที่เนื้อหาซ้ำเป๊ะ)
4. **ช่องเสริมที่กรอกไม่ได้กิน 15 วินาทีต่อช่อง** (แก้: ลดเหลือ 4 วิ + มาร์คช่อง "สาขา" ที่ DCTK เติมเองเป็น computed)
5. **บันทึกหน้า 1 ไม่ผ่าน — วันที่ส่งออกหาย** การกรอกช่องเสริมทำให้ DCTK คำนวณใหม่แล้วล้างวันที่ทิ้ง จึงขึ้น `DEPARTURE_DATE = NULL` (แก้: ย้ายช่องบังคับไปตั้งท้ายสุด + อ่านค่าย้อนกลับ ซ่อมเองได้ 3 รอบ)
6. **Save ที่ไม่ผ่านเงียบสนิท** ไปพังหน้าถัดไปด้วย timeout ที่ไม่บอกสาเหตุ (แก้: อ่านกล่องแดงของ DCTK ออกมาลงล็อก)

---

## ✅ รันจริงครบ 3 หน้าแล้ว — ใบร่าง DCTK000035375

รันแบบ `RPA_NO_FINALIZE=1` (บันทึกทุกหน้าจริง แต่ไม่ finalize/ไม่พิมพ์/ไม่ส่งอีเมล)

| หน้า | ช่องเสริมที่กรอกได้ |
|---|---|
| หน้า 1 · ใบขนสินค้าขาออก | 13 ช่อง |
| หน้า 2 · ใบกำกับสินค้า | 31 ช่อง |
| หน้า 3 · ส่วนรายละเอียด | 52 ช่อง |
| **รวม** | **96 ช่อง** (นอกเหนือจากช่องเดิมที่ระบบกรอกอยู่แล้ว) |

ผล: `total=3 done=1 errors=0`

> ⚠️ **ต้องเข้าไปลบใบร่าง DCTK000035375 ในระบบกรมฯ ด้วย** — เป็นใบทดสอบ ยังไม่ได้ส่งกรมฯ

### โหมดทดสอบมี 3 ระดับ

| คำสั่ง | ทำอะไร |
|---|---|
| `RPA_DRY_RUN=1` | กรอกอย่างเดียว ไม่กด Save เลย (ทดสอบได้แค่หน้า 1) |
| `RPA_NO_FINALIZE=1` | บันทึกครบทุกหน้าจริง แต่ไม่ส่งกรมฯ — เหลือใบร่างต้องลบเอง |
| ไม่ใส่อะไร | รันจริง ส่งกรมฯ + พิมพ์ + ส่งอีเมล |

ใส่ `RPA_ONLY_ROWS=<เลขแถว>` เพื่อรันทีละใบ

---

## ~~ข้อ 1 — รัน SQL~~ ✅ ทำแล้ว

รันครบทั้ง 2 ไฟล์แล้ว ตรวจสอบแล้วผ่าน:

| ลำดับ | ไฟล์ | เพิ่มอะไร |
|---|---|---|
| 1 | `rpa-web/sql/11_extra_fields_and_masters.sql` | `extra_fields`, `field_modes`, ตาราง `declaration_templates` |
| 2 | `rpa-web/sql/12_master_3_levels.sql` | `consignee_names`, `product_codes`, `priority` (Master 3 ระดับ) |

**วิธีตรวจว่าสำเร็จ:** เปิดเว็บ → เมนู "คลัง Master" ต้องไม่ขึ้นข้อความเตือนสีแดง

---

## 🟠 ข้อ 2 — ทวงเอกสารจากคุณแพว/คุณแนท (Action item จากที่ประชุม)

ที่ตกลงไว้แต่ยังไม่ได้รับ:

1. **เอกสาร Invoice / Packing ของธนากร 3 consignee**
   เริ่มจาก consignee ที่มีสินค้าชนิดเดียวคงที่ (คุณแพวบอกจะให้น้องหาให้)
2. **เลขที่ใบกำกับสินค้า** ของแต่ละ consignee เพื่อดึงใบเดิมจาก DCTK มาทำ Master

ในโฟลเดอร์ตอนนี้มีแค่ 2 ไฟล์ (`FOB-DKN 22.2026.pdf`, `TH01128238 BKG-J&C.pdf`)
ในฐานข้อมูลมี 2 ใบ (ZECK TSE/2607083, THANAKORN/DKN 22/2026) และ Master 1 อัน — ยังไม่พอทำ 3 consignee

> 💡 **เรื่องเลขที่ใช้ค้นหา** — ตอบตามที่คุณถามไว้: ใช้ **"เลขที่ใบกำกับสินค้า"** ถูกแล้วครับ
> เพราะเป็นเลขบนเอกสารจริงที่ลูกค้ารู้จัก ส่วน "เลขที่อ้างอิง" (DCTK000034914) ระบบรันเอง
> **แต่ผมทำให้ใส่ได้ทั้ง 3 แบบ** — ระบบดูรูปแบบเลขแล้วเลือกคอลัมน์ค้นหาให้เอง
> (`DCTK…` = เลขอ้างอิง · `A005-16905-…` = เลขใบขน · ที่เหลือ = เลขใบกำกับ)

---

## 🟡 ข้อ 3 — ให้คุณแพวเซ็ต Master ชุดแรก

ระบบรองรับครบแล้ว แต่ *"consignee นี้ใช้พิกัดอะไร หน่วยอะไร สิทธิ์อะไร"* เป็นความรู้ของคุณแพว

**ทางที่เร็วที่สุด** — พอได้เลขใบกำกับจากข้อ 2 แล้ว ให้ผมดึงจาก DCTK มาให้อัตโนมัติ:

```bash
cd rpa-import-node
set -a; . ../rpa-web/.env; set +a
PULL_INVOICE="DKN 22/2026" PULL_CUSTOMER=THANAKORN node dist/pull-master-cli.js
```

ทดสอบแล้วใช้ได้จริง — ดึงมาได้ **84 ช่องหัวใบ + 1 รายการสินค้า × 79 ช่อง**
พร้อมจับ Consignee (`DK&N VIETNAM LTD`) และรหัสสินค้า (`REFINED BLEACHED`) ให้อัตโนมัติ
(ใส่ `PULL_DRY=1` ถ้าอยากดูผลก่อนโดยไม่บันทึก)

จากนั้นคุณแพวเปิดหน้า **คลัง Master** แล้วปรับ:
- **โหมดรายช่อง** — ช่องไหน "ใช้ค่า Master" (ทับ AI) · "จาก AI" · "ไม่กรอก"
  เช่น หน่วยน้ำหนัก = ใช้ค่า Master (KGM ตายตัว) · น้ำหนัก = จาก AI
- **Consignee ที่ใช้ Master นี้** — ใส่ได้หลายราย คั่นด้วยจุลภาค
  (ตรงกับที่คุณแพวบอก "3 consignee ใช้ชื่อสินค้าเดียวกัน ให้อ่านต้นแบบตัวเดียวกัน")
- **รหัสสินค้า** — ถ้า consignee เดียวกันแต่คนละสินค้า ให้แยก Master

---

## ✅ สิ่งที่ทำเสร็จแล้วเมื่อคืน

### ตรงกับที่ตกลงในที่ประชุมทุกข้อ

| ที่ประชุมขอ | ทำแล้ว |
|---|---|
| UI เลียนแบบระบบศุลกากร "เป๊ะ ๆ ทุกช่อง" | **324 ช่อง** ครบทุกแท็บ · ป้ายชื่อไทยตรงระบบจริง · จัดกลุ่มตามหน้าจอจริง · ช่องบังคับมี \* แดง |
| ย้าย Shipping Mark มาหน้าแรก | อยู่หน้า 1 กลุ่ม "การขนส่ง" |
| แยกน้ำหนักสุทธิ/รวม + หน่วยแยกช่อง | ครบ 4 ช่อง ตั้ง preset ได้ทุกช่อง |
| แยกปริมาณในใบขน/ใบกำกับ + หน่วยแยก | ครบ 4 ช่อง + มีรายการหน่วยจริง 2,182 ค่าไว้ตรวจ |
| **"ต้องมีหน้า 4"** | แยกแท็บ **หน้า 4 · สิทธิประโยชน์/ภาษีอากร** แล้ว (33 ช่อง) |
| ช่องรหัสสินค้า · ชื่อไทย/อังกฤษ · พิกัด | เปิดครบ ตั้ง preset ได้ |
| **Preset 3 ระดับ** | ลูกค้า → Consignee (หลายราย/Master) → รหัสสินค้า |

### ตรรกะเลือก Master 3 ระดับ (ทดสอบแล้ว)

| เอกสารที่เข้ามา | Master ที่ระบบเลือก |
|---|---|
| DK&N + REFINED BLEACHED | Master A (ตรงทั้ง consignee + สินค้า) |
| DK&N + RBD PALM OLEIN | Master B (consignee เดียวกัน คนละสินค้า) |
| ABC CO + REFINED BLEACHED | Master A (3 consignee ใช้ต้นแบบเดียวกัน) |
| consignee ใหม่ไม่เคยเจอ | ค่าเริ่มต้นของลูกค้า |

### ระบบตรวจข้อมูลแบบเดียวกับกรมฯ (ถอดกฎมาจากระบบจริง)

| ชนิดกฎ | จำนวน |
|---|---|
| ช่องบังคับ · ความยาวสูงสุด · ชนิดข้อมูล | 162 ช่อง |
| ความสัมพันธ์ "ถ้า X → Y" | 40 |
| ตารางเงื่อนไข Incoterms | 15 แบบ |
| กฎข้ามช่อง (เช่น ติ๊ก BOI ต้องมีเลขบัตร) | 7 |
| กฎกระทบยอด (ควบคุม vs รายละเอียด) | 13 รายการ |
| รายการค่าที่กรมฯ ยอมรับ | 2,931 ค่า |

ตัวอย่างที่ระบบจะบล็อกก่อนส่ง RPA: วันที่ใบกำกับเกิน ETD · หน่วย `BAG`/`TON` ที่ไม่มีในระบบกรมฯ ·
เที่ยวเรือยาวเกิน 12 ตัว · CFR แต่รายการมีค่าประกัน · ติ๊ก BOI แต่ไม่ใส่เลขบัตร

---

## ⚠️ ความเสี่ยงที่ต้องรู้

**ทดสอบครบ 3 หน้าแล้ว** — ตัวกรอกช่องเสริมทำงานทั้ง 96 ช่อง
สิ่งที่ยังไม่ได้ทดสอบคือขั้น finalize/พิมพ์/ส่งอีเมล ซึ่งเป็นโค้ดเดิมที่ใช้งานจริงอยู่แล้ว

ถ้าจะรันจริงครั้งแรก ให้ใส่ `RPA_ONLY_ROWS=<แถว>` เพื่อทำทีละใบ และดูล็อกทุกบรรทัด

---

## 📁 เครื่องมือใหม่ที่สร้างไว้ (รันซ้ำได้เมื่อกรมฯ เปลี่ยนระบบ)

| คำสั่ง | ทำอะไร |
|---|---|
| `node dist/pull-master-cli.js` | ดึงใบเดิมจาก DCTK → Master (ใช้บ่อยสุด) |
| `node dist/survey-cli.js` | สำรวจช่องกรอกทั้ง 3 หน้า |
| `node dist/rules-cli.js` | ถอดกฎ (บังคับ/ความยาว/ถ้า X แล้ว Y) |
| `node dist/combo-lists-cli.js` | ดึงรายการค่าที่กรมฯ ยอมรับ |
| `node dist/term-factor-cli.js` | ตารางเงื่อนไข Incoterms |
| `node dist/survey-doctypes-cli.js` | ตรวจว่าชนิดเอกสารต่างกันไหม |

รายละเอียดทั้งหมดอยู่ใน `rpa-web/FIELDS_AND_MASTERS.md`
